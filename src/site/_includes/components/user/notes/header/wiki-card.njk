<div style="background:red;padding:0.5rem;color:#fff;">HEADER slot activo</div>

<h4 style="margin-top:1rem;">DEBUG DE VARIABLES DISPONIBLES</h4>
<pre style="background:#222;color:#0f0;padding:1rem;overflow:auto;">
<b>renderData:</b>
{{ renderData | dump }}
</pre>

<pre style="background:#222;color:#ff0;padding:1rem;overflow:auto;">
<b>note:</b>
{{ note | dump }}
</pre>

<pre style="background:#222;color:#0ff;padding:1rem;overflow:auto;">
<b>page:</b>
{{ page | dump }}
</pre>

<script>
document.addEventListener('DOMContentLoaded', function() {
    console.log("DEBUG FRONTMATTER");
    console.log("script tag:", document.getElementById("user-note-frontmatter"));

    // Intentamos parsear frontmatter si el script existe
    const scriptTag = document.getElementById("user-note-frontmatter");
    if(scriptTag) {
        try {
            const rawData = scriptTag.textContent.trim();
            const frontmatter = JSON.parse(rawData);
            console.log("frontmatter parseado:", frontmatter);
        } catch(e) {
            console.error("Error al parsear frontmatter:", e);
        }
    } else {
        console.log("No se encontró #user-note-frontmatter");
    }
});
</script>
