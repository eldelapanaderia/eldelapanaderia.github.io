{# Este componente se inyecta en el slot 'filetree/top' de la barra lateral. #}

<div class="custom-tag-index-wrapper">
    
    <h2>Índice de Etiquetas</h2>
    
    <div class="tag-list">
        {# Itera sobre la colección de etiquetas de Eleventy. Asumimos que collections.tagList existe. #}
        {% for tag in collections.tagList | sort %}
            {% set tagUrl %}/tags/{{ tag }}/{% endset %}
            
            <div class="tag-link-wrapper">
                <a href="{{ tagUrl | url }}">{{ tag }}</a> 
                {# collections[tag] es la lista de notas que tienen esa etiqueta #}
                <span class="tag-count">({{ collections[tag] | length }})</span>
            </div>
        {% endfor %}
    </div>

</div>

{# 
    NOTAS:
    - Este código es muy simple para evitar conflictos de sintaxis o variables.
    - Las variables del componente original (x-data, menuItem, filetree) no existen en este slot, por eso las omitimos.
#}
